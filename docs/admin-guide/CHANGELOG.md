# Admin Guide Changelog

| Date | Change Summary | Frontend Impact | Backend Impact | Verification | Remaining Risk/TODO |
|---|---|---|---|---|---|
| 2026-05-25 | Tightened backup code auth input handling: login fallback now normalizes pasted code to digits and enforces exact 6-digit format client/server side. | Fewer failed retries on mobile copy/paste flows because separators/spaces are ignored and invalid code lengths are blocked early. | No schema changes; updated login input validation and server-side OTP token normalization. | `npm run build` passed locally. | Still requires live iPhone browser + mail-client smoke test to confirm real-world paste ergonomics. |
| 2026-05-25 | Upgraded timeline usability across schedules with live `Now` marker, `current/next` block emphasis, and admin conflict-overlap detection messaging. Added save-time overlap guardrail that blocks accidental conflicts unless `Allow overlap` is explicitly selected. Extended timeline view into staff schedule page. | Admins can spot dependency collisions before publishing and are prompted to resolve overlaps before save; staff/students can quickly orient to what is happening now and what is next on a calendar-like timeline. | No schema changes; shared timeline component and admin schedule server actions enhanced across admin/student/staff schedule screens. | `npm run build` passed locally. | Live visual QA with real high-density schedules is still needed to calibrate overlap messaging readability on smaller phones. |
| 2026-05-25 | Added backup sign-in code path on `/login` so users can complete auth with email + code when magic-link browser handoff fails. Updated email template to include a visible backup code block and updated operator/technical SOP docs. | Login now offers both `Send magic link` and `Sign in with code` paths, reducing mobile/webview friction. | Added `verifyOtp(email, token, type=email)` server action flow in login page; no schema change. | `npm run build` passed locally. | Requires production Supabase email template update from `supabase/templates/magic-link.html` to expose backup code in live emails. |
| 2026-05-25 | Hardened magic-link confirm flow for mobile/webview edge cases: `/auth/confirm` now safely parses both query params and URL-fragment auth params before forwarding to callback, and callback supports token-pair session restoration fallback. | Users landing from email clients that rewrite auth payloads into fragments now get a smoother “finishing sign-in” handoff instead of dead-end login loops. | Replaced `app/auth/confirm` route handler with confirm page redirect layer; extended `app/auth/callback` to handle `setSession` with `access_token` + `refresh_token` fallback in addition to PKCE `code` and `token_hash` flows. | `npm run build` passed locally. | Live-device validation still needed across iPhone Gmail in-app browser and Safari to confirm improved behavior in production email client contexts. |
| 2026-05-25 | Improved mobile magic-link retry UX by persisting the last attempted sign-in email and auto-prefilling it on return to login after errors/expiration. | Login retries are faster on phone because admins/staff/students do not need to retype their email after a failed or expired link flow. | Added lightweight first-party cookie (`torch_last_login_email`) set by login server action and read on login page render. | `npm run build` passed locally. | Still depends on user opening newest link in the same browser context when email clients use embedded webviews. |
| 2026-05-25 | Added learner-facing calendar-esque agenda timeline to student schedule with duration-scaled blocks, right-side time scale, and location color legend for faster at-a-glance readability. | Student schedule now presents a visual timeline first, then detail list/table for quick scan plus deeper reference on mobile and desktop. | No DB changes. Reused existing timeline component and styling with student-facing placement updates. | `npm run build` passed locally. | Live browser/device QA still recommended to tune spacing/readability with real program-day data volume. |
| 2026-05-25 | Overhauled schedule model/UX for operations readiness: added staff-only Friday day (`day 0`) with Friday–Tuesday labels, changed schedule ordering to start-time-first, added preconfigured location dropdowns, added live end-time preview in add/edit forms, and added duration-scaled timeline view with right-side time scale and location color coding. | Schedule screens now show explicit start/end ranges, weekday labels, location-based visual grouping, and a calendar-style timeline to reveal dependencies quickly on mobile and desktop. | Added migration `20260526000500_torch_live_staff_day_zero.sql` to allow `day_number` 0 for staff schedule table while preserving student day constraints; schedule queries now sort by `start_time` first. | `npm run build` passed locally. | Production Supabase migration still needs applying before staff Friday (`day 0`) writes are accepted in production DB. |
| 2026-05-25 | Added admin edit flows for existing announcements and resources (categories/items), plus confirm prompts for destructive announcement/resource removals using reusable client confirm-submit control. Updated operator and technical docs to match new workflows. | Admins can now correct existing content in place from mobile or desktop, and destructive actions require explicit confirmation before submit. | Added authenticated update server actions for `announcements`, `resource_categories`, and `resource_items`; added reusable `components/ui/ConfirmSubmitButton.js` for guarded submit behavior. | `npm run build` passed locally. | Confirmation prompts depend on browser `window.confirm`; still requires live-device QA for exact iOS browser behavior. |
| 2026-05-25 | Added admin cleanup controls for live operations: remove announcements, remove resource items, and remove resource categories. Added mobile-friendly action layout for these controls and updated operator/technical docs accordingly. | Admin announcement/resource cards now expose direct remove actions that are easier to use on phone-sized screens. | Added authenticated server actions for deleting `announcements`, `resource_items`, and year-scoped `resource_categories` from admin pages. | `npm run build` passed locally. | Category removal is destructive for its nested items (`ON DELETE CASCADE`); operators should remove categories only when intentional. |
| 2026-05-25 | Hardened magic-link login failure handling for PKCE cross-browser errors (`PKCE code verifier not found`) with user-friendly recovery messaging, added callback support for `token_hash` + `type` verification alongside auth-code exchange, switched app redirect target to `/auth/confirm`, and updated Supabase template guidance to token-hash links. Updated admin troubleshooting docs for this error class. | Login error banners now show actionable guidance instead of raw Supabase technical text when the link opens in a different browser/device context. | `app/auth/callback` now supports both `exchangeCodeForSession(code)` and `verifyOtp({ token_hash, type })` paths, with `/auth/confirm` forwarding parameters and template docs aligned to token-hash flow. | `npm run build` passed locally. | Supabase Dashboard Magic Link template must be updated from `supabase/templates/magic-link.html` in production settings. |
| 2026-05-25 | Added living admin guide system, SOP runbooks, and update protocol. Added admin users bulk upload/edit/deactivate/remove and year tooling. Added schedule admin CRUD/year tools. Added legal pages and legal links. | `/admin/users`, `/admin/schedule`, login/app footer legal links, `/legal/*` pages | Service-role admin actions for user lifecycle; year-aware admin operations; middleware legal-route allowlist | `npm run build` passed | Multi-year membership model still single-profile-per-user; plan migration to membership table |
| 2026-05-25 | Refactored admin docs into role-based tracks: `Portal Operators` (no technical context) and `Technical Owners` (backend/deploy/risk). Added plain-language quickstart and rebuilt legal/risk checklist as a structured intake. | Documentation now maps directly to admin portal labels and actions for non-technical helpers. | Technical guide now isolated from frontline SOPs; legal checklist now captures required compliance inputs before policy finalization. | `npm run build` passed. | Need Bryan to complete legal/risk intake items before final compliance drafts and counsel packet can be produced. |
| 2026-05-25 | Fixed admin user-action redirect handling (`NEXT_REDIRECT` false error) and added migration to restore RLS helper functions to `SECURITY DEFINER` to resolve student/staff `stack depth limit exceeded` query failures. | Admin `Create User`, `Import Users`, and `Save Changes` now redirect cleanly on success without showing `NEXT_REDIRECT` as an error banner. | Added DB migration `20260525233000_torch_live_fix_rls_helper_definer.sql` to correct RLS helper execution context. | `npm run build` passed locally. | Supabase migration still needs to be applied to the remote project before student/staff pages recover in production. |
| 2026-05-25 | Applied Bryan-provided legal profile details and converted legal/risk docs into an operational compliance baseline with retention schedule, incident timeline, accessibility process, and vendor/data-sharing governance cadence. Updated public legal page language to reflect age scope and operational-only messaging. | Updated `/legal/privacy`, `/legal/terms`, `/legal/safety` content to match TORCH operating model and contact details. | Added policy baselines and recurring compliance operations in `docs/legal-risk-checklist.md` and `docs/admin-guide/SOP-RUNBOOKS.md`. | `npm run build` passed. | Counsel review still required before declaring legal final; open decisions listed in legal checklist section 7. |
| 2026-05-25 | Applied production Supabase migration `torch_live_fix_rls_helper_definer` and deployed production build `dpl_H4fuZ5eewMphUx499fCDVLkbbjk1`. | Student/staff data pages should no longer hit `stack depth limit exceeded`; admin user actions should no longer show false `NEXT_REDIRECT` error banners. | Live DB migration list now includes `20260525233534 torch_live_fix_rls_helper_definer`; production deployment is aliased to `live.torchleadershipacademy.org`. | Verified `GET /api/health` = 200 and `GET /login` = 200 on custom domain; deployment status `Ready`. | Needs authenticated student/staff re-login smoke test in browser to fully confirm end-user behavior. |
